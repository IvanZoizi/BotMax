# 🎯 FOCUS Bot - Ваш персональный помощник продуктивности в MAX

<div align="center">

![Productivity](https://img.shields.io/badge/Productivity-Boost-green)
![Python](https://img.shields.io/badge/Python-3.12+-blue)
![Docker](https://img.shields.io/badge/Docker-Ready-important)
![MAX Messenger](https://img.shields.io/badge/MAX-Messenger-orange)

**Первый бот продуктивности, который работает там, где вы уже проводите время**

[Особенности](#-ключевые-возможности) • [Установка](#-установка-и-запуск) • [Использование](#-использование)

</div>

## 🚀 О проекте

**FOCUS** — это интеллектуальный бот для мессенджера MAX, созданный для борьбы с прокрастинацией и повышения личной эффективности. Современные люди постоянно отвлекаются, теряют фокус и не могут системно двигаться к своим целям, несмотря на все попытки самоорганизации. FOCUS решает эту проблему!

### 💡 Что делает FOCUS особенным?

- 🎯 **Умное планирование** - Ставьте цели и разбивайте их на achievable-шаги
- ⏱️ **Pomodoro-таймер** - 25 минут полной погруженности в задачи
- 📊 **Визуальная статистика** - Отслеживайте рост и достижения
- 🔔 **Интеллектуальные напоминания** - Бот проводит вас от цели к результату

## 🎪 Ключевые возможности

| Функция | Описание |
|---------|-----------|
| **🎯 Постановка целей** | Разбивайте большие цели на achievable-шаги с помощью умного помощника |
| **⏰ Pomodoro-таймер** | Работайте концентрационно с помощью встроенного таймера (25 минут работы / 5 минут отдыха) |
| **📈 Трекинг привычек** | Контроль посещаемости и наглядная статистика вашего роста |
| **🔔 Умные уведомления** | Напоминания о задачах в нужное время, регулярные чекапы прямо в мессенджере |

## 🛠️ Установка и запуск

### Предварительные требования

- Python 3.12+
- Docker и Docker Compose (для Docker-установки)
- [UV](https://github.com/astral-sh/uv) (опционально)
- Аккаунт в мессенджере MAX

### Способ 1: Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd botmax

# Создайте файл конфигурации
cp .env.example .env
# Отредактируйте .env файл, добавив ваши настройки

# Запустите приложение
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f bot
```

**docker-compose.yml:**
```yaml
services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-bot}
      - POSTGRES_USER=${POSTGRES_USER:-bot_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "postgres"

  bot:
    build: .
    container_name: bot
    user: appuser
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${POSTGRES_NAME:-bot}
      - DB_USER=${POSTGRES_USER:-bot_user}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
        labels: "bot"

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge
```

### Способ 2: Использование UV

```bash
# Установите UV если еще не установлен
curl -LsSf https://astral.sh/uv/install.sh | sh

# Клонируйте репозиторий
git clone <your-repo-url>
cd botmax

# Установите зависимости
uv sync

# Активируйте виртуальное окружение
uv run python -m botmax

# Или запустите напрямую
uv run botmax
```

### Способ 3: Классический pip

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd botmax

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/MacOS
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python -m botmax
```

## ⚙️ Конфигурация

Создайте файл `.env` в корневой директории:

```env
TOKEN=your-token-bot

POSTGRES_PASSWORD=admin
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_DB=focus
```

## 🎮 Использование

### Основные команды бота:

```
/start - Начать работу с ботом
```

### Пример рабочего процесса:

1. **🎯 Ставим цель**

2. **📝 Разбиваем на шаги**

3. **⏰ Работаем по Pomodoro**

4. **📊 Отслеживаем прогресс**


## 🏗️ Архитектура

```
BotMax/

```



## 📈 Roadmap

- [ ] Интеграция с AI для умных рекомендаций
- [ ] Расширенная аналитика продуктивности
- [ ] Мобильное приложение-компаньон
- [ ] Интеграция с календарями
- [ ] Gamification элементов

---

<div align="center">

**Начните свой путь к продуктивности сегодня!** 🚀

*FOCUS Bot - там, где ваши цели становятся реальностью*

</div>